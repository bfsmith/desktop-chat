import { IAppConfig } from './config';

let config: IAppConfig = {
	appName: "utest",
	port: 3001
};

// config.port = Math.floor(Math.random() * 50000 + 10000); // [10000 - 60000]

export default config;
